[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Put the repo root on sys.path so `import src...` works regardless of whether
# pytest is invoked as `pytest` (console script) or `python -m pytest`.
pythonpath = .
